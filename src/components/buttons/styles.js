import { StyleSheet } from "react-native"

export default StyleSheet.create({

  wrap: {
    padding: 10,
    marginTop: 5,
    marginBottom: 5,
    alignItems: "center",
  },

  label: { color: "white" },

})
