import { StyleSheet } from "react-native"

export default StyleSheet.create({

  wrap: {
    paddingLeft: 20,
    paddingRight: 20,
  },

  row: {
    display: "flex",
    flexDirection: "row",
    
  }

})
